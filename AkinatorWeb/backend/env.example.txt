# ===========================================
# Configuration Akinator Twitch Web
# ===========================================
# IMPORTANT : copiez ce fichier en `.env` et remplissez les valeurs.
#   cp env.example.txt .env
# NE JAMAIS commiter le fichier .env !
# ===========================================

# --- Serveur ---
PORT=3000
NODE_ENV=development

# --- JWT (REQUIS) ---
# Générez une clé forte, ex : openssl rand -hex 64
JWT_SECRET=votre_cle_secrete_tres_longue_minimum_64_caracteres_aleatoires

# --- Secrets applicatifs (indépendants de JWT_SECRET) ---
# REQUIS en production : sans ces clés, le serveur refuse de démarrer
# (fail-secure — voir services/encryption.js et services/auditService.js).
# En dev/test uniquement, une clé de repli dérivée de JWT_SECRET est utilisée
# (avec avertissement), pour ne pas bloquer le développement local.
# Génération : node scripts/generate-keys.js
ENCRYPTION_KEY=
AUDIT_HMAC_KEY=
IP_HASH_SALT=

# --- Compte administrateur ---
# Utilisé par les scripts de création d'admin (backend/scripts/).
# ADMIN_PASSWORD est REQUIS pour la création du compte admin : si absent,
# la création est simplement ignorée au démarrage (aucun mot de passe par défaut).
ADMIN_USERNAME=Akinator
ADMIN_PASSWORD=votre_mot_de_passe_admin

# --- Twitch / IGDB (optionnel) ---
# Sans ces clés, les recommandations enrichies via IGDB sont désactivées.
# https://dev.twitch.tv/console
TWITCH_CLIENT_ID=votre_client_id
TWITCH_CLIENT_SECRET=votre_client_secret

# --- Données persistantes ---
# DATA_DIR contient la base SQLite (akinator.db) et les avatars uploadés.
# En local, laisser vide : backend/data est utilisé par défaut.
# En production, pointer sur le disque monté, sinon TOUT est perdu à chaque
# redéploiement (ex. Render Persistent Disk : DATA_DIR=/var/data).
# DATA_DIR=/var/data

# Surcharges fines (facultatives, prioritaires sur DATA_DIR) :
# DATABASE_PATH=./data/akinator.db
# AVATARS_DIR=./data/avatars

# --- Réglages de sécurité (optionnel) ---
# Valeurs par défaut appliquées si non définies.
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
# BCRYPT_ROUNDS=12
# CORS_ORIGIN=*
